{
  "name": "functions",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.1",
    "firebase-functions": "^5.0.1",
    "googleapis": "^140.0.1",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
